[pytest]
# test_cnn_live.py is a demo script, not a test: it matches pytest's discovery
# pattern and builds the CNN at import time, which breaks collection when no
# checkpoint exists. Restrict discovery to the real suite.
testpaths = tests
